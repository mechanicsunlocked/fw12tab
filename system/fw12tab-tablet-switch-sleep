#!/bin/bash
# systemd-sleep hook: re-bind the SW_TABLET_MODE switch after resume — it can
# drop on wake from s2idle/deep/hibernate the same way it does at boot.
# Installed as /usr/lib/systemd/system-sleep/fw12tab-tablet-switch.
[ "${1:-}" = post ] && /usr/lib/fw12tab/bind-tablet-switch
exit 0
